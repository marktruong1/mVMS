<%= form_with(model: reason,  local: true, class: 'form-horizontal') do |f| %>
  <% if reason.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(reason.errors.count, "error") %> prohibited this reason from being saved:</h2>

      <ul>
      <% reason.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="form-group">
      <%= f.label 'Name', class: 'col-md-2 control-label' %>
      <div class="col-md-9">
        <%= f.text_field :name, class: 'form-control' %>
      </div>
    </div>

    <div class="form-group" >
      <div class="col-md-11">
        <%= link_to 'Cancel', reasons_path, class: 'btn btn-warning pull-right' %>
        <%= f.submit 'Save', class: 'btn btn-success pull-right' %>
      </div>
    </div>
<% end %>
